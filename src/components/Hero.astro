---
// src/components/Hero.astro
---

<section class="hero">
    <div class="hero-content">
        <h1 >Rooted to Rise</h1>
        <div class="hero-subtitle">African Heritage, African Future</div>
        <p>We create long-term value through sustainability, blending African wisdom with modern ESG frameworks to foster resilient communities and ethical business practices.</p>
        <div class="cta-buttons">
            <a href="/contact" class="btn btn-primary">Book ESG Audit</a>
            <a href="/assessment" class="btn btn-secondary">Community Assessment</a>
        </div>
    </div>
</section>

<style>
    .hero {
        background: linear-gradient(rgba(54, 69, 79, 0.8), rgba(178, 34, 34, 0.8)),
                    url('https://picsum.photos/1920/1080?random=1');
        background-size: cover;
        background-position: center;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: white;
    }

    .hero-content {
        max-width: 800px;
        padding: 2rem;
    }

    .hero h1 {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        color: white;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .hero-subtitle {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        color: var(--ochre);
        font-weight: 600;
    }

    .hero p {
        font-size: 1.2rem;
        margin-bottom: 2rem;
        color: rgba(255,255,255,0.9);
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    @media (max-width: 768px) {
        .hero h1 {
            font-size: 2.5rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
        }

        .cta-buttons {
            flex-direction: column;
            align-items: center;
        }
    }
</style>